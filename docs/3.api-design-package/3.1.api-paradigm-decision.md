# 3. API design

## 3.1 API paradigm decision

---

for MafricaFix mvp, i’m using **REST over HTTPS with JSON**:

* simple, easy to read and debug
* every frontend (web) already speaks HTTP + JSON
* no strong need for GraphQL or gRPC here (data shapes are pretty simple)